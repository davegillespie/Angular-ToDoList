"use strict";
// getter syntax
angular.module("todoApp", []);


// Use $scope.$broadcast when transmitting data from
// parent → child .
// Use $scope.$emit when transmitting data from
// child → parent .
// Use $rootScope.$broadcast when transmitting data
// from parent → parent (meaning they aren't nested).